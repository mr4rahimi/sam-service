---
const issues = [
  {
    q: "دستگاه روشن نمی شود",
    a: "معمولا مشکل از باتری، مدار تغذیه، شارژر یا آی سی پاور است. ابتدا شارژر و کابل را تست کنید. اگر چراغ یا ویبره ندارید، احتمال خرابی مدار پاور یا اتصالات داخلی زیاد است و نیاز به بررسی تخصصی دارد.",
    tags: ["پاور", "باتری", "برد"],
    icon: "⚡",
  },
  {
    q: "باتری زود خالی می کند یا شارژ نمی شود",
    a: "این مشکل می تواند از باتری فرسوده، سوکت شارژ، آی سی شارژ یا تنظیمات مصرف انرژی باشد. اگر درصد شارژ ناگهانی کم و زیاد می شود، باتری یا مدار شارژ باید تست شود.",
    tags: ["شارژ", "سوکت", "آی سی شارژ"],
    icon: "🔋",
  },
  {
    q: "دستگاه کند است یا هنگ می کند",
    a: "علت رایج شامل پر بودن حافظه، برنامه های سنگین، داغ شدن، یا خرابی حافظه داخلی است. ابتدا فضای ذخیره سازی را خالی کنید و برنامه های اضافی را حذف کنید. اگر مشکل ادامه داشت، نیاز به بررسی سخت افزار و سلامت حافظه وجود دارد.",
    tags: ["کندی", "حافظه", "بهینه سازی"],
    icon: "🐢",
  },
  {
    q: "تصویر قطع می شود یا صفحه نمایش مشکل دارد",
    a: "سیاه شدن صفحه، خطوط، لکه یا پرش تصویر می تواند از پنل، فلت، برد تصویر یا ضربه و فشار باشد. اگر با تکان دادن دستگاه تصویر می آید و می رود، احتمال مشکل فلت یا کانکتور وجود دارد.",
    tags: ["LCD", "فلت", "تصویر"],
    icon: "🖥️",
  },
  {
    q: "دستگاه داغ می کند و ناگهانی خاموش می شود",
    a: "معمولا به خاطر گرد و غبار، خمیر سیلیکون خشک شده، خرابی فن، یا فشار پردازشی است. اگر بعد از چند دقیقه استفاده خاموش می شود، سیستم خنک کاری باید سرویس و تست شود.",
    tags: ["دما", "فن", "خنک کاری"],
    icon: "🌡️",
  },
  {
    q: "کیبورد، تاچ پد یا پورت ها کار نمی کنند",
    a: "مشکل می تواند از نفوذ رطوبت، ضربه، خرابی کابل یا آی سی ورودی باشد. پورت شارژ و USB اگر لق شده یا قطع و وصل می شود، بهتر است قبل از آسیب بیشتر تعمیر شود.",
    tags: ["کیبورد", "تاچ پد", "USB"],
    icon: "⌨️",
  },

  // موارد اضافه برای سئو و پوشش گسترده تر
  {
    q: "وای فای یا بلوتوث وصل نمی شود",
    a: "ابتدا شبکه را فراموش و دوباره اضافه کنید و تنظیمات شبکه را ریست کنید. اگر همچنان سیگنال ضعیف است یا اصلا روشن نمی شود، احتمال خرابی آنتن، ماژول ارتباطی یا برد وجود دارد.",
    tags: ["WiFi", "Bluetooth", "آنتن"],
    icon: "📶",
  },
  {
    q: "دوربین باز نمی شود یا تصویر تار است",
    a: "علت می تواند کثیفی لنز، ضربه، مشکل نرم افزاری یا خرابی ماژول دوربین باشد. اگر خطا می دهد یا فوکوس نمی کند، تست ماژول و تعویض در صورت نیاز انجام می شود.",
    tags: ["دوربین", "فوکوس", "لنز"],
    icon: "📷",
  },
  {
    q: "اسپیکر صدا ندارد یا صدا خش دار است",
    a: "گاهی گرد و غبار یا رطوبت باعث افت صدا می شود. اگر با هندزفری صدا دارید ولی از بلندگو ندارید، احتمال خرابی اسپیکر یا مسیر صوتی است و باید تست شود.",
    tags: ["صدا", "اسپیکر", "میکروفن"],
    icon: "🔊",
  },
  {
    q: "میکروفن کار نمی کند یا طرف مقابل صدای شما را ندارد",
    a: "ابتدا برنامه تماس و مجوزها را چک کنید. اگر در همه برنامه ها مشکل دارید، میکروفن یا مسیر صوتی و آی سی مربوطه باید بررسی شود.",
    tags: ["میکروفن", "تماس", "مسیر صوتی"],
    icon: "🎙️",
  },
  {
    q: "دستگاه آب خورده یا رطوبت دیده",
    a: "سریع خاموش کنید و به هیچ وجه شارژ نکنید. خشک کردن سطحی کافی نیست و باید دستگاه باز شود، شستشو تخصصی برد انجام شود و قطعات حساس تست شوند تا از سولفاته شدن جلوگیری شود.",
    tags: ["آب خوردگی", "سولفاته", "برد"],
    icon: "💧",
  },
  {
    q: "دستگاه ریستارت می شود یا روی لوگو گیر می کند",
    a: "این حالت می تواند از مشکل نرم افزاری، خرابی حافظه، یا نوسان تغذیه باشد. اگر بعد از آپدیت رخ داده، با تعمیر نرم افزاری و فلش استاندارد حل می شود. اگر تکرار شد، بررسی سخت افزار لازم است.",
    tags: ["بوت", "ریستارت", "نرم افزار"],
    icon: "🔁",
  },
  {
    q: "خطای شارژ سریع یا شارژ کند است",
    a: "کابل و شارژر استاندارد بسیار مهم است. اگر شارژ سریع فعال نمی شود، ممکن است سوکت شارژ، مدار شارژ یا مسیر دیتا مشکل داشته باشد. تست آمپر و ولتاژ دقیق انجام می شود.",
    tags: ["شارژ سریع", "آداپتور", "سوکت"],
    icon: "⚡",
  },
  {
    q: "تاچ صفحه درست کار نمی کند یا پرش دارد",
    a: "گاهی مشکل از گلس، ضربه، نفوذ رطوبت یا ایراد پنل تاچ است. اگر نقاطی از صفحه لمس نمی شود، احتمال خرابی تاچ یا فلت زیاد است.",
    tags: ["تاچ", "پنل", "فلت"],
    icon: "🫳",
  },
  {
    q: "خط و خش یا شکستگی صفحه نمایش",
    a: "اگر تصویر سالم است ولی گلس ترک خورده، باید بررسی شود که امکان تعویض گلس وجود دارد یا نیاز به تعویض کامل پنل است. تعویض اصولی باعث حفظ کیفیت تصویر و تاچ می شود.",
    tags: ["گلس", "LCD", "تعویض"],
    icon: "🧩",
  },
];
---

<section class="ci mt-10" aria-label="ایرادات رایج دستگاه های سامسونگ">
  <div class="ci-shell led-surface">
    <div class="ci-led" aria-hidden="true"></div>

    <div class="ci-head">
      <div>
        <h2 class="ci-title">ایرادات رایج دستگاه های سامسونگ</h2>
        <p class="ci-sub">عیب یابی تخصصی و اعلام هزینه قبل از تعمیر</p>
      </div>

      <div class="ci-badge" aria-hidden="true">
        <span class="dot"></span>
        پاسخ های کوتاه و کاربردی
      </div>
    </div>

    <div class="ci-grid" role="list">
      {issues.map((it, i) => (
        <details class="ci-card led-ring" role="listitem" name="issue-accordion">
          <summary class="ci-sum">
            <span class="ci-ico" aria-hidden="true">{it.icon}</span>
            <span class="ci-q">{it.q}</span>
            <span class="ci-chevron" aria-hidden="true">⌄</span>
          </summary>

          <div class="ci-body">
            <p class="ci-a">{it.a}</p>

            <div class="ci-tags" aria-label="برچسب ها">
              {it.tags.map((t) => (
                <span class="ci-tag">{t}</span>
              ))}
            </div>
          </div>
        </details>
      ))}
    </div>

    <div class="ci-foot">
      <div class="ci-footCard">
        <div class="ci-footTitle">اگر مشکل شما در لیست نبود</div>
        <p class="ci-footText">
          کافی است تماس بگیرید تا دستگاه شما به صورت دقیق عیب یابی شود و هزینه قبل از تعمیر اعلام شود.
        </p>
      </div>

      <a class="ci-cta led-ring" href="tel:02191300348">تماس و عیب یابی سریع</a>
    </div>
  </div>
</section>

<style>
  .ci {
    --sx: 0, 200, 255;
    --ink: rgba(241, 245, 249, 0.96);
    --muted: rgba(226, 232, 240, 0.75);
  }

  .ci-shell {
    position: relative;
    border-radius: 28px;
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(255,255,255,0.04);
    padding: 18px;
    overflow: hidden;
    box-shadow: 0 26px 90px rgba(0,0,0,0.42), inset 0 1px 0 rgba(255,255,255,0.12);
  }

  .ci-led {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
    padding: 2px;
    background: conic-gradient(
      from var(--a),
      rgba(var(--sx), 0) 0deg,
      rgba(var(--sx), 0) 290deg,
      rgba(var(--sx), 0.55) 312deg,
      rgba(var(--sx), 1.0) 328deg,
      rgba(var(--sx), 0.55) 344deg,
      rgba(var(--sx), 0) 360deg
    );
    -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
    -webkit-mask-composite: xor;
            mask-composite: exclude;
    mix-blend-mode: screen;
    filter: saturate(1.25);
    opacity: 0.65;
    animation: ci-rot 7.0s linear infinite;
  }

  @property --a {
    syntax: "<angle>";
    inherits: false;
    initial-value: 0deg;
  }
  @keyframes ci-rot { to { --a: 360deg; } }

  .ci-head {
    position: relative;
    z-index: 1;
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
    padding: 2px 2px 10px;
  }

  .ci-title {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 950;
    color: var(--ink);
  }

  .ci-sub {
    margin: 6px 0 0;
    font-size: 0.92rem;
    color: var(--muted);
    line-height: 1.8;
  }

  .ci-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 12px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(2,6,23,0.40);
    color: rgba(226,232,240,0.88);
    font-size: 0.82rem;
    font-weight: 900;
    white-space: nowrap;
  }

  .ci-badge .dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: rgba(var(--sx), 0.95);
    box-shadow: 0 0 18px rgba(var(--sx), 0.55);
  }

  .ci-grid {
    position: relative;
    z-index: 1;
    margin-top: 10px;
    display: grid;
    gap: 12px;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .ci-grid { grid-template-columns: 1fr 1fr; }
  }

  @media (min-width: 1024px) {
    .ci-grid { grid-template-columns: 1fr 1fr 1fr; }
  }

  /* Card */
  .ci-card {
    border-radius: 22px;
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(2, 6, 23, 0.40);
    box-shadow: 0 18px 60px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.12);
    overflow: hidden;
    transition: transform 200ms ease, background-color 200ms ease, border-color 200ms ease;
  }

  @media (hover: hover) and (pointer: fine) {
    .ci-card:hover {
      transform: translateY(-2px);
      background: rgba(2, 6, 23, 0.48);
      border-color: rgba(255,255,255,0.14);
    }
  }

  .ci-sum {
    list-style: none;
    cursor: pointer;
    display: grid;
    grid-template-columns: 44px 1fr 22px;
    align-items: center;
    gap: 10px;
    padding: 14px 14px;
    user-select: none;
  }

  .ci-sum::-webkit-details-marker { display: none; }

  .ci-ico {
    width: 40px;
    height: 40px;
    border-radius: 16px;
    display: grid;
    place-items: center;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(255,255,255,0.03);
    font-size: 1.05rem;
  }

  .ci-q {
    color: rgba(241,245,249,0.96);
    font-weight: 950;
    font-size: 0.95rem;
    line-height: 1.7;
  }

  .ci-chevron {
    color: rgba(226,232,240,0.75);
    transition: transform 200ms ease;
    transform: translateY(-1px);
  }

  details[open] .ci-chevron {
    transform: rotate(180deg);
  }

  .ci-body {
    padding: 0 14px 14px;
    border-top: 1px solid rgba(255,255,255,0.08);
  }

  .ci-a {
    margin: 12px 0 0;
    color: rgba(226,232,240,0.80);
    font-size: 0.9rem;
    line-height: 1.9;
  }

  .ci-tags {
    margin-top: 12px;
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .ci-tag {
    font-size: 0.72rem;
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(255,255,255,0.03);
    color: rgba(203,213,225,0.9);
  }

  /* Footer */
  .ci-foot {
    position: relative;
    z-index: 1;
    margin-top: 14px;
    display: grid;
    gap: 12px;
    grid-template-columns: 1fr;
    align-items: stretch;
  }

  @media (min-width: 900px) {
    .ci-foot { grid-template-columns: 1fr auto; }
  }

  .ci-footCard {
    border-radius: 22px;
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(255,255,255,0.03);
    padding: 14px;
  }

  .ci-footTitle {
    font-weight: 950;
    color: rgba(241,245,249,0.96);
  }

  .ci-footText {
    margin: 8px 0 0;
    color: rgba(226,232,240,0.78);
    line-height: 1.9;
    font-size: 0.9rem;
  }

  .ci-cta {
    border-radius: 22px;
    padding: 14px 16px;
    font-weight: 950;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 200, 255, 0.92);
    color: rgba(2, 6, 23, 0.95);
    border: 1px solid rgba(0, 200, 255, 0.35);
    transition: transform 180ms ease, background-color 180ms ease;
    white-space: nowrap;
  }

  .ci-cta:hover {
    transform: translateY(-1px);
    background: rgba(90, 225, 255, 0.98);
  }

  /* ----- LED ring reusable ----- */
  .led-ring { position: relative; overflow: hidden; }
  .led-ring::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    padding: 2px;
    pointer-events: none;
    background: conic-gradient(
      from var(--b),
      rgba(var(--sx), 0) 0deg,
      rgba(var(--sx), 0) 292deg,
      rgba(var(--sx), 0.45) 314deg,
      rgba(var(--sx), 0.95) 330deg,
      rgba(var(--sx), 0.45) 346deg,
      rgba(var(--sx), 0) 360deg
    );
    -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
    -webkit-mask-composite: xor;
            mask-composite: exclude;
    mix-blend-mode: screen;
    filter: saturate(1.25);
    opacity: 0.9;
    animation: ci-rot2 6.2s linear infinite;
  }
  .led-ring::after {
    content: "";
    position: absolute;
    inset: -30%;
    border-radius: inherit;
    pointer-events: none;
    background: conic-gradient(
      from var(--b),
      rgba(var(--sx), 0) 0deg,
      rgba(var(--sx), 0) 305deg,
      rgba(var(--sx), 0.30) 326deg,
      rgba(var(--sx), 0.60) 340deg,
      rgba(var(--sx), 0.30) 354deg,
      rgba(var(--sx), 0) 360deg
    );
    filter: blur(16px) saturate(1.25);
    opacity: 0.50;
    mix-blend-mode: screen;
    animation: ci-rot2 6.2s linear infinite;
  }

  @property --b {
    syntax: "<angle>";
    inherits: false;
    initial-value: 0deg;
  }
  @keyframes ci-rot2 { to { --b: 360deg; } }

  @media (prefers-reduced-motion: reduce) {
    .ci-led, .led-ring::before, .led-ring::after { animation: none; }
  }
</style>
